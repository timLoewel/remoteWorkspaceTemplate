name: start development server

on:
  workflow_dispatch:

jobs:
  run-scripts:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        pwd
        python -m pip install --upgrade pip
        pip install -r ./infrastructure/developmentServer/scripts/requirements.txt

    - name: Start development server
      env:
        HETZNER_API_TOKEN: ${{ secrets.HETZNER_API_TOKEN }}
      run: python ./infrastructure/developmentServer/scripts/create_small_development_server.py

    # - name: Set up Ansible
    #   uses: ansible/ansible@v2.9.0
    #   with:
    #     ansible-version: '2.9.0'

    # - name: Install necessary dependencies
    #   run: ansible-playbook ../infrastructure/developmentServer/ansible/playbook.yml